import { Routes } from '@angular/router';
import { TelBotComponent } from './tel-bot/tel-bot.component';

export const routes: Routes = [

    {
        path: "telegram",
        component: TelBotComponent
    }

];
